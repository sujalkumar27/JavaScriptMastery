"use strict";// means that the code uses updated features of JavaScript

// Datatypes
// 1. Primitive datatypes: number, string, boolean, null, undefined, symbol, bigint
// 2. Non-primitive datatypes: object (array, function, object)

let name="Sujal"; // string 
let age=20; // number   size 2 to the power of 53
let isApproved=true; // boolean
let firstName=null; // null
let lastName=undefined; // undefined
let selectedColor; // undefined

console.log(typeof name);
console.log(typeof age);
console.log(typeof isApproved);
console.log(typeof firstName);
console.log(typeof lastName);
console.log(typeof selectedColor);


